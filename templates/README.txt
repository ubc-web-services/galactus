Twig Templates in this directory have been sorted to reflect the structure of the default theme, Classy, which includes all core templates.

To override or add a new Twig template, copy it into the appropriate directory here and run 'drush cr' (assuming twigdebug is enabled in your sites/[insertsiteanme]/services.yml file).
